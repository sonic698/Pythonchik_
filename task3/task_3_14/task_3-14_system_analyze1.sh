#!/bin/bash

echo "Анализ использования дискового пространства"

printf "%-20s %-10s %-10s %s\n" "Файловая система" "Использовано" "Число" "Статус"

df -h | awk 'NR>1 {
    fs=$1
    usage_str=$5

    # Убираем % и преобразуем в число
    num = usage_str
    gsub(/%/, "", num)
    num = num + 0
    # Определяем статус
    if (num > 90) {
        status = "КРИТИЧНО!"
    } else {
        status = "OK"
    }
